# net/ — Multiplayer (planned)

Not built yet. Will hold the client-side networking: WebSocket connection to the
authoritative game server (shared session + puzzle state) and WebRTC voice
signalling. See `docs/ARCHITECTURE.md` → *Multiplayer plan*.
