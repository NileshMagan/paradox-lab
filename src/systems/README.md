# systems/ — Runtime systems (mostly planned)

Cross-cutting systems that run every frame or on events:
- `audio/` — per-dimension soundscapes (Alpha: drips/roots; Beta: server hum).
- `interaction/` — raycast pick + "use" on interactive prop slots.
- `puzzle/` — puzzle graph and cross-dimension effects (server-authoritative).
